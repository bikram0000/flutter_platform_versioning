name: flutter_platform_versioning
description: Flutter Platform Versioning is a command-line tool to set different versions for different platforms in a Flutter project.
version: 1.0.1
repository: https://github.com/bikram0000/flutter_platform_versioning.git

environment:
  sdk: ^3.0.5

# Add regular dependencies here.
dependencies:
  args: ^2.4.0
  logger: ^2.0.1
  yaml: ^3.1.1


dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
